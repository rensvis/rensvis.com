---
import PageLayout from '../layouts/PageLayout.astro';
import { SITE } from '../lib/site';
---

<PageLayout title={`About | ${SITE.name}`} description={SITE.description}>
  <main class="container mx-auto max-w-2xl px-4 py-16">
    <h1 class="text-2xl font-bold">About</h1>
    <p class="mt-4 text-muted-foreground">{SITE.description}</p>
    <p class="mt-4 text-muted-foreground">
      This page will be expanded with a fuller bio and links in a later phase.
    </p>
  </main>
</PageLayout>
