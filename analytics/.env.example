# Required - use postgres hostname for docker-compose (replace with actual URL for Railway)
DATABASE_URL=postgresql://umami:umami@postgres:5432/umami

# Required for Umami (generate: openssl rand -base64 32)
# Note: Modern Umami uses APP_SECRET; HASH_SALT is deprecated
APP_SECRET=generate-random-string
